<%= link_to "Listdapp", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', new_user_session_path %>  
    </li>
    <li>
    <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
  </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Account Management', edit_user_registration_path %>
    </li>
	  <% if current_user.admin? %>
	Logged in as admin: <%= current_user.email %>.
	<% elsif current_user.partner? %>
	Logged in as partner: <%= current_user.email %>.
	<% else %>
	<li>
    <%= link_to 'My Passes', '/mypasses' %>
    </li>
	Logged in as user: <%= current_user.email %>.

	<% end %>
  <% else %>
    <li>
    <%= link_to 'Sign up', new_user_registration_path %>
    </li>
  <% end %>
</ul>