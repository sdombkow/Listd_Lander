<ul class="nav pull-right">
  <% if user_signed_in? %>
	<li class="dropdown">
	  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
	    <% if current_user.admin? %>
		  <%= current_user.email %>.
		<% elsif current_user.partner? %>
	      <%= current_user.email %>.
		<% else %>
		  <%= current_user.email %>.
		<% end %>
	    <b class="caret"></b>
	  </a>
	  <ul class="dropdown-menu">
		<li> <%= link_to 'Account Management', edit_user_registration_path %> </li>
		<li> <%= link_to 'Payment Management', '/payment_management' %> </li>
		<% if !current_user.admin? && !current_user.partner? %>
      	  <li> <%= link_to 'My Passes', '/mypasses' %> </li>
		<% end %>
		<li class="divider"></li>
		<li> <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %> </li>
  	  </ul>
	</li>
	
  <% else %>
      <li> <%= link_to 'Sign Up', new_user_registration_path %> </li>
  	  <%= render 'layouts/login' %>
  <% end %>
</ul>
<div class="navbar-search pull-right">
  <%= form_tag :controller => 'bars', :action => 'search', :method => 'get' do %>
    <%= text_field_tag :search, params[:search], :id => 'search_field appendedInputButton', :class => "search-query search-length", :placeholder => "Find a bar" %>
  <% end %>
</div>
